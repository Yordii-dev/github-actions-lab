name: Workflow - Practicing Github Actions
on: push
env:
  GREETING: ${{secrets.GREETING}}
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: Step without action
        run: echo "commit"

      - name: action CHECKOUT REPO
        uses: actions/checkout@v4

      - name: SHA ENVIRONMENT
        run: echo $GITHUB_SHAç

      - name: REF ENVIRONMENT
        run: |
          REF=$GITHUB_REF 
          echo $REF

      - name: SECRETS
        run: echo ${{secrets.GREETING}}

      - name: SECRET STORED IN WORKFLOW VAR
        run: |
          set -u
          echo $GREETING

      - name: SECRET STORED IN WORKFLOW VAR
        run: |
          set -u
          echo $GREETING
        env:
          GREETING: "Hola"
